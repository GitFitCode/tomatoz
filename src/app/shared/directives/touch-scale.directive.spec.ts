import { TouchScaleDirective } from './touch-scale.directive';

describe('TouchScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchScaleDirective();
    expect(directive).toBeTruthy();
  });
});
